# pipeline

Deno CLI：`dish-list` · `fetch` · `extract` · `normalize` · `rewrite` · `validate` · `load` · `review` · `eval`。
与 `supabase/functions/_shared/` 共用模块（D75）。脚手架从 T1.2 开始搭建，本 ticket（T0.1）只占位。见 `docs/PLAN.md` §7、`docs/TICKETS.md` T1.2。
